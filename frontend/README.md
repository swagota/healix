# HEALIX Frontend

From this folder:

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:5000` by default. Set `VITE_API_URL` in `.env` if the backend uses another URL.
